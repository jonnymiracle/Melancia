/**
 * The code handed out for a first email signup.
 *
 * Kept in sync by hand with the Shopify discount of the same name. It is
 * active, 15% off, with no usage cap and no once-per-customer limit, so it was
 * already shareable the moment it left in an email. Showing it on the page
 * costs nothing it had not already spent.
 *
 * If you swap it for a per-customer code, this constant has to go and the
 * popup needs a real code-generation step behind it.
 */
export const WELCOME_CODE = 'MELANCIA15'
export const WELCOME_PERCENT = 15
